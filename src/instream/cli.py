"""CLI entry point for inSTREAM (stub — implemented in Phase 10)."""


def main():
    raise NotImplementedError(
        "CLI not yet implemented. Use the Python API:\n"
        "  from instream.io.config import load_config\n"
        "  config = load_config('configs/example_a.yaml')"
    )
